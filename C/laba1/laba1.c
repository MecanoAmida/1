#include <stdio.h>
#include <math.h>

double log(double d)

int main(void) {
    double a, x, g, f, y;

    print("Введите x и a/n");
    scanf("%lf", &x);
    scanf("%lf", &a);
    g = -((8*(45*a**2+32*a*x-20*x**2))/(63*a**2+109*a*x+36*x**2))
    f = 1/(math.cos(25*a**2+30*a*x+9*x**2))
    y = 1/(math.log(10*a**2+43*a*x+12*x**2+1))
    printf ("g= %f \n", g);
    printf ("f= %f \n", f);
    printf ("y= %f \n", y);

    return 0

}
